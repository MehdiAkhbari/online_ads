# Runtime dependencies for the adsim research code.
# Install with:  pip install -r requirements.txt
# For the package itself (editable):  pip install -e .
# For notebooks: pip install -e ".[notebooks]"

numpy>=1.26,<2.0
pandas>=2.2,<2.3
scipy>=1.11,<2.0
scikit-learn>=1.4,<1.6
econml>=0.15.1,<0.16
joblib>=1.3
matplotlib>=3.8
pyreadstat>=1.2
